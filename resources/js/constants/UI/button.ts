import {makeStyles} from "@mui/styles";
export const buttonStyles = makeStyles({
    main: {
        display: 'flex',
        justifyContent: 'center'
    },
    button: {
        marginTop: '15px',
        width: '50%',
    },
})