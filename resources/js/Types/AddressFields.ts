export type AddressFields = {
    country: string,
    region: string,
    settlement: string,
    street: string,
    house: string,
    postal_code: string,
    flat?: string,
    block?: string,
    area?: string
}